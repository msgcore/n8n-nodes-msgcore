name: Publish n8n Node to npm

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm install

      - name: Build package
        run: npm run build

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Get package version
        id: version
        run: echo "VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.VERSION }}
          name: n8n Node Release v${{ steps.version.outputs.VERSION }}
          body: |
            ## n8n-nodes-msgcore v${{ steps.version.outputs.VERSION }}

            Auto-generated from MsgCore backend contracts.

            ### Installation in n8n
            ```bash
            npm install n8n-nodes-msgcore@${{ steps.version.outputs.VERSION }}
            ```

            Or add to your n8n instance's package.json.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
